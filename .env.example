# Choose one provider. Only that provider's key is required for analysis.
REACT_RECON_AI_PROVIDER=openai
REACT_RECON_AI_MODEL=gpt-5.6-luna
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional overrides used by resume, analyze, reprocess, and report.
REACT_RECON_DATABASE=react-recon.db
REACT_RECON_EVIDENCE_DIR=evidence
